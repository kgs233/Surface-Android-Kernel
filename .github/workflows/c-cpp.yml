name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: config
      run: make x86_64_defconfig
    - name: make bzImage
      run: make bzImage
    - name: make modules
      run: make modules
